DATABASE_URL="file:./dev.db"
JWT_SECRET="change-this-secret-in-production"
PORT=4000

# Email (optional). If not set, emails are logged to the console/DB instead of sent for real.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM="attendance-system@example.com"
